import { PlayerMasterPageControl } from './player-master-page-control';

describe('PlayerMasterPageControl', () => {
  it('should create an instance', () => {
    expect(new PlayerMasterPageControl()).toBeTruthy();
  });
});
